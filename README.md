# Contact Management Web App 📇

## Overview
This project is a **contact management web application** built with **HTML and JavaScript**.  
It provides functionality for adding, editing, and managing contact information, with support for API key configuration to enable secure data storage or integration.

## Features
- ➕ **Add Contacts**: Create new contact entries via `add-contact.html`.
- ✏️ **Edit Contacts**: Update existing contact details through `edit-contact.html`.
- 🔑 **API Integration**: Enter and configure API keys using `enter-api-key.html` and `config.js`.
- 🏠 **Dashboard**: `index.html` serves as the main interface for managing contacts.

## Technologies Used
- **HTML** (96.5%) – structure and content.
- **JavaScript** (3.5%) – logic and interactivity.

## Project Structure
